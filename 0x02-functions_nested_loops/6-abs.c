#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @n: an integer passed
 * Return: an integer value
 */

int _abs(int n)
{
	if (-n)
	{
		return (n);
	}
	else
	{
		return (n);
	}
}
